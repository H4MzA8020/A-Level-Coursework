# Contains all classes. Import classes into main.

class Unit:
    def __init__(self, morale, org, atk, def, spd, strength, height, width, isPanicked)
                 self.morale = morale
                 self.org = org  # organisation.
                 self.atk = atk  # attack.
                 self.def = def # defense.
                 self.spd = spd # speed.
                 self.strength = strength
                 self.height = height
                 self.width = width
                 self.isPanicked = isPanicked

